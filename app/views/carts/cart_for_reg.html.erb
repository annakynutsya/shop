<% @cart_reg.each do |cart_item| %>
    <br>
    <%= cart_item.name %>
    <%= cart_item.price %>
    <%= button_to "Remove from cart", remove_from_cart_path(cart_item), method: :delete %>
  <% end %>