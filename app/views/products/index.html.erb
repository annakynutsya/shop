<div class="max-w-7xl mx-auto">
  <%= image_tag 'banner.jpg', class: "w-full cursor-pointer" %>
  <% @products.each do |product| %>
    <div class="product-card">
      <%= render 'shared_links/product',  product: product  %>
    </div>
  <% end %>
</div>
